"use client";

/**
 * Re-export shared UI Button. Single source of truth for button primitive.
 * Supports leftIcon, rightIcon, loading, isLoading, size="icon", variant="destructive".
 */
export { Button } from "@/components/ui";
export type { ButtonProps, ButtonVariant, ButtonSize } from "@/components/ui";
