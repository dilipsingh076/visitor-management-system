/**
 * Marketing / public layout. Use for home, about, contact, faq, features, how-it-works, privacy, use-cases.
 * Does not affect URL (route group).
 */
export default function MarketingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
