{
  "name": "vms-mcp-visualize",
  "version": "1.0.0",
  "description": "MCP server to visualize and inspect the Visitor Management System UI (login, signup, register society)",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "vms-mcp-visualize": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
