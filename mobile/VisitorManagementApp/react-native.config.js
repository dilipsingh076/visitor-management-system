module.exports = {
  // MaterialDesignIcons.ttf is shipped by @react-native-vector-icons/material-design-icons:
  // iOS: CocoaPods pod resources; Android: the package's Gradle copyFonts task.
  // Do not list fonts in `assets` — `npx react-native-asset` duplicates the file on iOS
  // (Xcode: "Multiple commands produce MaterialDesignIcons.ttf").
  assets: [],
};
